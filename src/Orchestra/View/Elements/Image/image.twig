<img
    src="{{src}}"
    {{html_attributes(
        {
            'srcset': srcset|join(', '),
            'sizes': sizes ?? null,
            'alt': altText ?? null,
            'title': title ?? null,
            'width': width ?? null,
            'height': height ?? null
        },
        attributes ?? {}
    )}}
>
