<img
    src="{{src}}"
    {{html_attributes(
        {
            'srcset': srcset|join(', '),
            'sizes': sizes ?? null,
            'alt': altText ?? null,
            'title': title ?? null
        },
        attributes ?? {}
    )}}
>
